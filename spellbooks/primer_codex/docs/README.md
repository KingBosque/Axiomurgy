# primer_codex spellbook

This spellbook packages the bundled primer transcripts into a single codex artifact.

Entrypoint:
- `publish_codex`

What it demonstrates:
- spellbook manifest loading
- deterministic validator runes
- proof-carrying witnesses
- packaged artifacts that stay inside the spellbook directory
